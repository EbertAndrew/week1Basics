var overallObj = {
    name: "Hello",
    name2: "abcdefg",
    name3: "ab"
    
}

function generIC(name, name2, name3){
    console.log(name + name2 + name3);
    if (str.length() < 2) return str;
        return str.substring(0, 2);
}