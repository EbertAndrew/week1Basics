var word = "absurd";

    var count = 500;

    while (count > 0)   {
        console.log(count + " " + word + " amounts of lag in the console");
        console.log(count + " " + word + " amounts of lag,");
        console.log("Process some out, load a string,");
        count = count - 1;

    if (count > 0)  {

        console.log(count + " " + word + " amounts of lag in the console.");
        
    }   else {
        console.log("No more " + word + " amounts of lag in the console.");
    }
    
}